<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.takoyakki.backend.domain.challenge.repository.DailyChallengeRankingsMapper">

    <insert id="insertDailyChallengeRanking" parameterType="java.util.List">
        INSERT INTO daily_challenge_rankings
        (
            member_id,
            rank_level,
            points,
            date,
            updated_at
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.memberId},
                #{item.rank},
                #{item.totalPoints},
                #{item.date},
                NOW()
            )
        </foreach>
    </insert>

    <select id="selectChallengeRanksByMemberId" resultType="java.util.List">
        SELECT
            member_id,
            rank_level AS rank,
            points AS totalPoints,
            date
        FROM
            daily_challenge_rankings
        WHERE
            member_id = #{memberId}
        ORDER BY
            date DESC
    </select>

</mapper>