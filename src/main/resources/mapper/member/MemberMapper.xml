<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.takoyakki.backend.domain.member.repository.MemberMapper">

        <resultMap id="MemberSelectResponseMap" type="MemberSelectResponseDto">
            <result property="memberId" column="member_id" />
            <result property="memberName" column="member_name" />
            <result property="phoneNumber" column="phone_number" />
        </resultMap>

        <select id="selectMemberInfo" parameterType="java.lang.Long" resultMap="MemberSelectResponseMap">
            SELECT
                member_id,
                id,
                member_name,
                role,
                email,
                nickname,
                phone_number
            FROM
                members
            WHERE
                member_id = #{memberId}
        </select>

        <update id="updateMemberInfo" parameterType="MemberUpdateRequestDto">
            UPDATE
                members
            SET
                email = #{email},
                nickname = #{nickname},
                phone_number = #{phoneNumber}
            WHERE
                member_id = #{memberId}
        </update>
    </mapper>